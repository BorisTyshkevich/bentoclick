{
  "ch_url":       "${CH_URL}",
  "mcp_url":      "${MCP_URL}",
  "spa_origin":   "${SPA_ORIGIN}",
  "db":           "${DB}",
  "brand_name":   "${BRAND_NAME}",
  "email_domain": "${EMAIL_DOMAIN}",
  "accent":       "${ACCENT}"
}
